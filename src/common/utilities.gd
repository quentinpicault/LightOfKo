extends Node

enum InputType { KEYBOARD, JOYPAD }
enum Facing { LEFT, RIGHT }
enum State { FEAR, IDLE, RUN, JUMP, FALL }
enum Track { TITLE, INTRO, SAD, HAPPY,STAGE_WITH_MASK, STAGE_WITHOUT_MASK, GET_MASK }
